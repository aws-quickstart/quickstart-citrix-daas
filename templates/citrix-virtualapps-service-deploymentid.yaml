AWSTemplateFormatVersion: '2010-09-09'

Description: This template deploys an IAM User to be used with Citrix MCS and Hosting Configuration as CTX140429.

Resources:
  # Lambda Function for generating the kubeadm token
  GenCitrixCloudDeploymentID:
    Type: "AWS::Lambda::Function"
    Properties:
      Code:
        ZipFile: |
          import random
          import string
          import cfnresponse
          def id_generator(size, chars=string.ascii_lowercase + string.digits):
            return ''.join(random.choice(chars) for _ in range(size))
          def handler(event, context):
            if event['RequestType'] == 'Delete':
              cfnresponse.send(event, context, cfnresponse.SUCCESS, {})
            if event['RequestType'] == 'Create':
              deployment_string = ("%s" % (id_generator(8)))
              responseData = {}
              responseData['DeploymentID'] = deployment_id
              cfnresponse.send(event, context, cfnresponse.SUCCESS, responseData)
              return token
      Handler: "index.handler"
      Runtime: "python2.7"
      Timeout: "5"
      Role: !GetAtt LambdaExecutionRole.Arn

  # A Custom resource that uses the lambda function to generate a random deployment id to be used in the naming of Citrix Cloud components.
  CitrixCloudQSDeploymentID:
    Type: "Custom::GenerateCitrixCloudQSDeploymentID"
    Version: "1.0"
    Properties:
      GenCitrixCloudDeploymentID: !GetAtt GenCitrixCloudDeploymentID.Arn

  # IAM role for Lambda function for generating kubeadm token
  LambdaExecutionRole:
    Type: "AWS::IAM::Role"
    Properties:
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
        - Effect: "Allow"
          Principal:
            Service: ["lambda.amazonaws.com"]
          Action: "sts:AssumeRole"
      Path: "/"
      Policies:
      - PolicyName: "lambda_policy"
        PolicyDocument:
          Version: "2012-10-17"
          Statement:
          - Effect: "Allow"
            Action:
            - "logs:CreateLogGroup"
            - "logs:CreateLogStream"
            - "logs:PutLogEvents"
            Resource: "arn:aws:logs:*:*:*"
  
  AccessKeyParameter:
    Type: "AWS::SSM::Parameter"
    Properties:
      Name: !Sub 'CitrixQSDeploymentID-${AWS::StackName}'
      Type: "String"
      Value: !GetAtt CitrixCloudQSDeploymentID.DeploymentID
      Description: "IAM Access Key used for Citrix MCS Hosting Configuration"            